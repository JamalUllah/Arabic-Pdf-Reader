# core/__init__.py
# Marks "core" as a Python package (allows: from core.pdf_document import PdfDocument).
# Core = PDF/OCR/save logic only — no buttons or windows.
# Library reference for all imports: see imports_guide.py in the project root.
